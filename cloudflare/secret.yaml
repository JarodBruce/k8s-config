# このYAMLは直接適用しません。
# 以下のコマンドを実行して、.envファイルからSecretを作成してください。
#
# 1. .env.example を .env にコピーします。
#    cp .env.example .env
#
# 2. .env ファイル内の CLOUDFLARE_TOKEN をご自身のトークンに書き換えます。
#
# 3. 以下のコマンドを実行して Secret を作成します。
#    kubectl create secret generic cloudflare-tunnel-secret --from-env-file=.env -n default
#
# ---
#
# 参考: 生成されるSecretのマニフェスト例
# apiVersion: v1
# kind: Secret
# metadata:
#   name: cloudflare-tunnel-secret
#   namespace: default
# type: Opaque
# data:
#   CLOUDFLARE_TOKEN: <base64エンコードされたトークン>
